#pragma once

namespace mm {
    // at least one function to map a single physical page to a single virtual page address,
    // later come with a system of managing the virtual page tables automatically
}